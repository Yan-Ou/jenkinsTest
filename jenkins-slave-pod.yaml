metadata:
  labels:
    app: sre-toolbox
spec:
  containers:
    - name: toolbox
      image: ouyannz/sre-toolbox:latest
      command:
        - cat
      tty: true
      volumeMounts:
        - mountPath: /var/run/docker.sock
          name: docker-sock
  volumes:
    - name: docker-sock
      hostPath:
        path: /var/run/docker.sock
